# 마을AI사무장 (Village AI Secretary)

## 프로젝트 개요

전국의 마을(이장·사무장) 업무를 AI로 지원하는 커뮤니티 플랫폼.
마을 소식을 SNS 피드처럼 공유하고, 이장/사무장에게는 공고문 분석·영수증 변환·회의록 정리 등 AI 업무 도구를 제공한다.

## 기술 스택

| 구분 | 기술 |
|------|------|
| 프론트엔드 | React (Next.js / Vite) + TypeScript |
| 배포 | Vercel |
| 백엔드/DB | Firebase (Auth, Firestore, Storage, Functions) |
| 인증 | Firebase Auth — Google 로그인 |
| AI | Anthropic Claude API (문서 분석, 텍스트 변환, 회의록 정리, TTS 나레이션) |
| 스타일 | Tailwind CSS + 다크모드/라이트모드 토글 |

## 핵심 사용자 흐름

```
[비로그인] 메인 → 마을 소식 피드(공개) 열람
     ↓ 로그인 클릭
[Google 로그인] → 최초 사용자: 마을 지정(검색/매칭)
     ↓
[마을 지정 완료] → 마을 대시보드 진입
     ↓
[이장/사무장 권한] → AI 업무 도구 활성화
```

## 마을 생성 및 가입 흐름

### 최초 생성 (이장/사무장)
1. 마을 이름 입력 → 입력 필드에서 **펼침 목록(autocomplete)**으로 매칭
2. 행정안전부 / 마을종합지원센터 API 크롤링으로 기본 정보 자동 수집
3. 마을 기본 정보 + 대시보드로 마을 첫 페이지 자동 생성
4. 초대 링크 생성 가능

### 이후 가입 (마을 주민)
1. 마을 이름 검색 → 이미 생성된 마을 목록에서 선택
2. 가입 승인 후 마을 피드 및 기능 이용

## 주요 기능

### 1. 메인 화면 — 마을 소식 피드
- SNS 피드 형태로 마을 소식 표시
- 모바일 기준 화면 절반을 피드가 좌우 스와이프 형태로 구현
- 비로그인 상태에서도 공개 피드 열람 가능
- 상단: 마을 소식 / 이벤트 / 마을 특산품 탭
- 좋아요, 댓글, 공유 기능

### 2. 이장·사무장 대시보드 (로그인 후)
- 마을 소식 수, 이벤트, 마을회의, 자금현황 요약 카드
- 오늘 할 일 체크리스트
- AI 기능 바로가기 버튼
- 최근 업로드 사진 갤러리

### 3. AI 기능 (6가지)
각 기능은 대시보드에서 **버튼**으로 접근:

| # | 기능 | 설명 |
|---|------|------|
| 1 | 공고문 분석 | 공고문 이미지/PDF 업로드 → AI가 핵심 내용 추출·요약 |
| 2 | 영수증 → 엑셀 | 영수증 이미지 업로드 → OCR + AI로 엑셀 변환 + 대시보드 시각화 |
| 3 | 텍스트 → 양식 변환 | 텍스트/초안 입력 → 정해진 양식(.hwpx 등)으로 자동 변환 후 다운로드 |
| 4 | 회의록 자동 정리 | 녹음 파일 업로드 → STT + AI 요약으로 회의록 자동 생성 |
| 5 | 대신 읽어주기 | 마을 스피커 방송용 텍스트 → AI 나레이션 음성 생성 |
| 6 | 마을 문서함 | 생성된 문서·영수증·회의록 등 통합 관리 |

### 4. 인증 및 프로필
- 우측 상단: 로그인/로그아웃 상태 표시 + 회원가입 버튼
- 로그인 시 Google 로그인 → 마을 대시보드로 이동
- 프로필 클릭 → 마이페이지 → 업무모드 전환

### 5. 다크모드/라이트모드
- 토글 스위치로 전환
- 시스템 테마 자동 감지 지원

## 화면 구조 (페이지 목록)

```
/                     → 메인 (마을 소식 피드)
/login                → Google 로그인
/village/setup        → 마을 지정/생성
/village/[id]         → 마을 대시보드
/village/[id]/feed    → 마을 소식 피드 (상세)
/village/[id]/ai      → AI 기능 허브
/village/[id]/docs    → 문서함
/mypage               → 마이페이지/프로필
```

## 하단 네비게이션 (모바일)

| 아이콘 | 라벨 | 경로 |
|--------|------|------|
| 🏠 | 홈 | `/` |
| 📄 | 문서함 | `/village/[id]/docs` |
| 🔍 | 검색 | `/search` |
| 👤 | 마이 | `/mypage` |

## 문서 구조

```
claude.md                    ← 이 파일 (메인 스펙)
docs/
├── design.md               ← 디자인 시스템 및 UI 가이드
├── architecture.md         ← 기술 아키텍처 (Vercel + Firebase)
├── auth.md                 ← 인증 및 사용자 관리
├── village-setup.md        ← 마을 생성/가입 흐름
├── ai-features.md          ← AI 기능 상세 스펙
├── dashboard.md            ← 대시보드 화면 스펙
├── feed.md                 ← 마을 소식 피드 스펙
└── api.md                  ← API 및 데이터 모델
```

## 개발 우선순위

1. **Phase 1**: 프로젝트 셋업 + Google 로그인 + 마을 지정
2. **Phase 2**: 메인 피드 화면 (비로그인 공개 + 로그인 전용)
3. **Phase 3**: 대시보드 + 사진 업로드
4. **Phase 4**: AI 기능 버튼 UI + 개별 기능 구현
5. **Phase 5**: 다크모드 + 반응형 마무리

## 코딩 컨벤션

- TypeScript strict 모드
- 컴포넌트: PascalCase (`VillageFeed.tsx`)
- 유틸/훅: camelCase (`useVillage.ts`)
- Firebase 관련: `lib/firebase/` 하위에 모듈 분리
- AI 관련: `lib/ai/` 하위에 기능별 분리
- 커밋 메시지: `feat:`, `fix:`, `docs:`, `style:` prefix 사용
